version: 2

models:
  - name: int_customers
    group: hr
    access: private
    columns:
      - name: customer_id
        description: Id of the customer
      - name: first_name
      - name: last_name
      - name: full_name
